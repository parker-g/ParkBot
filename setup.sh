#!/usr/bin/env sh
# test script

python config/setup-wiz.py # creates venv, installs pip dependencies

# 2. download FFMPEG, nssm

# 3. check if FFMPEG is downloaded, if not then download FFMPEG and store it's executable as an env variable.

# 